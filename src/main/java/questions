mysql -u projet_gei_002 -p -h srv-bdens.insa-toulouse.fr
Lio0Uhah
use projet_gei_002;



PROBLEM DOESNT DELETE PROPERLY
PROBLEM POUR CREER NEWUSER

syst sql peut autoincrémenter
id comme un entier, randomly generated verifier qu'il n'existe pas,
(créer une var stat qu'on augmente de 1)




HOW IT WORKS
delete from user where idUser = 1;
select * from user;

pour update aller dans le m sur le côté droit et faire le bouton reload

pour connect to database, après faire use projet_gei_002;




Statement Database
/*connection*/
UseDatabase database = new UseDatabase();
Connection conn;
conn = database.connectToDatabase();
/*insertion*/
String insertSql =                               ///////////////// LE TRUC QU'ON FAIT
database.doStatementDatabase(conn,insertSql);
database.disconnectToDatabase(conn);


